'use client'

import React from 'react'

const SettingsForm = ({ id, name, chatBot, plan }: SettingsFormProps) => {
    //create and revoke the useSettings hook
    return (
        <div>
            
        </div>
    )
}

export default SettingsForm