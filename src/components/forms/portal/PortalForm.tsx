import React from 'react'


const PortalForm = ({
    questions,
    type,
    customerId,
    domainid,
    bookings,
    products,
    email,
    amount,
    stripeId,
}: PortalFormProps) => {
    // hooks: usePortal
    return (
        <div>
            portla form
        </div>
    )
}

export default PortalForm