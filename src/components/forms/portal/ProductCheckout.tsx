import React from 'react'

const PaymentCheckout = ({ onBack, products, onNext, amount, stripeId }: PaymentCheckoutProps) => {
    return (
        <div>

        </div>
    )
}

export default PaymentCheckout