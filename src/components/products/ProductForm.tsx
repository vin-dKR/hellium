'use client'

const CreateProductForm = ({ id }: ProductFormProps) => {
    // useProducts() hooks
    return (
        <form
            className="mt-3 w-full flex flex-col gap-5 py-10"
        // onSubmit={onCreateProduct} -- from hook
        >

        </form>
    )
}

export default CreateProductForm