type PlanUses = {
  plan: string;
  credits: number;
  domains: string[];
  clients: number;
};
const PlanUses = ({ plan, credits, domains, clients }: PlanUses) => {
  return <></>;
};

export default PlanUses;
